# Ledger — one line per wave

Appended at each wave end (protocol step 5). Columns:
`date | track wave | model | promised vs shipped | verify full-runs/flakes | ROI verdict`.
Trends live here so recurring costs (flakes, detours, budget creep) are visible
across sessions instead of rediscovered per wave.

| date | track / wave | model | promised vs shipped | runs/flakes | ROI |
|------|--------------|-------|---------------------|-------------|-----|
| 2026-07-16 | protocol + Tours tab (one-off) | Fable 5 | as briefed: 3 doc changes + exec-summary rule + Tours tab, backfill both lists (30 spots) | 1 targeted + 1 full / 0 flakes | high — one session unlocks the whole owed playtest backlog |
| 2026-07-16 | tour-guarantee fixes (one-off) | Fable 5 | audit found 6 broken spots (Bruno pushed past my first count of 3); all fixed + standing rule | 2 targeted + 1 full / 0 flakes; 1 check bug (first-vs-nearest bear), reasoned not rerun | high — Tours tab now delivers on every button; flares spot was dead as shipped |
| 2026-07-16 | turtle release readability (one-off) | Fable 5 | as approved: hatchling silhouette + per-morning event toast, both shipped with padre checks | 1 targeted + 1 full / 0 flakes | good — the event existed but was unreadable and silent after first log |
| 2026-07-16 | placement legality (one-off) | Fable 5 | audit (Bruno's play report): 15/15 Bucky's + 15/33 H-E-Buddy + 5/8 LSC on roads, HEB in bay/border/river, billboards on pavement, 5 landmarks straddling — all fixed via one legalize gate + HEB rejects + billboard re-snap + 5 nudges; 6 legality checks added | 3 targeted + 2 full / 4 stale raw-coord checks fixed in two passes (one found only by the full run) | high — every fix player-visible; offline audit mirror made the whole wave cheap to validate before touching the browser |
| 2026-07-16 | Bucky's night sign (one-off, 3 iterations) | Fable 5 | Bruno's night reads drove each step: dark-on-yellow text → neon-on-dark (LSC idiom, sign light 16→5) → tube-trace strokes with dark letter interiors; suite asserts neon wiring + day/night ramp | 3 targeted + 3 full / 0 flakes; 2 SHOTs on explicit request (new SHOT-gated pylon close-up) | good — sign is finally readable at night; two lit-surface attempts before the emissive route shows the visual-judgment loop needs the player (or a shot) early |
| 2026-07-16 | Band Parity spec session | Fable 5 | take on Bruno's band-parity directive → track opened: BAND_PARITY_SPEC.md, 6 waves, all 4 open calls resolved via options (tiers / tally / airports / rails deferred); BACKLOG entries annotated, W1 briefing written | 0 targeted + 1 full (docs-only session-end rule) / — | good — two of the four asks were already true (pop scaling, real DEM), diagnosis prevented two no-op waves |
| 2026-07-16 | Band Parity W1 — the network | Sonnet 5 | briefed tier fetch (motorway\|trunk\|primary) + rebake + coverage (140/177 within 25u) + connectivity check, shipped as specced; also root-caused and fixed Bruno's live density report (cross-bbox duplicate ways from the 4 overlapping state bboxes, id-deduped) plus two defects it surfaced: a Mexico/Juárez street leak (tier fetch has no country filter; clipped to neighbor-counties.json) and an I-30 monument silently lost to a ref-blind merge radius once I-49 joined the candidate set (shoulder.js dedup now requires matching ref) | 2 full + several targeted (band/shoulder/debug) / 0 flakes, 2 stale hardcoded counts fixed (shoulder.mjs monument range, data.test.mjs band-highway count) | high — shipped the briefed network AND fixed the reported bug at its root instead of patching the symptom; regression guards added for both defect classes (duplicate-polyline check, coverage floor) |
| 2026-07-16 | Band Parity W1 — OK secondary-tier top-up | Sonnet 5 | Bruno asked for full connectivity, not just the near-misses; measured that 33 of 37 gaps were OK towns 40-280u out (off primary/trunk/motorway entirely), so refetched OK with `secondary` added and folded it into the existing 'primary' render tier (Texas has no separate secondary tier) — coverage 140→166/177 (OK 73→97/104). Rebake surfaced two latent `deriveCrossings` bugs the smaller road set never exercised: FM/US roads that hug a straight state-line border for miles without crossing it were read as false crossing candidates (now requires a chain leave Texas, not just touch it), and a 14u secondary-tier stub running parallel to the OK/TX line broke the outward-neighbor tangent check (now requires ≥20u stub length) | 1 full + several targeted / 0 flakes; 3 sequential fix attempts on the crossing-candidacy bug before landing the right invariant (chain-wholly-in-Texas, then chain-must-straddle-both-sides — too strict, dropped legit crossings to 31 — then min-stub-length, which held) | high — 11 remaining gaps are all genuinely off any mapped road or a small near-miss; further gains would need AR/LA/NM secondary tiers, an explicit ask, not assumed scope |
| 2026-07-16 | Band Parity W1 — LA+NM secondary-tier top-up | Sonnet 5 | asked to go further: AR skipped (already 15/15), LA+NM refetched with secondary added — coverage 166→169/177 (LA 37→39/39, NM 17→18/19). Two real bugs, both self-inflicted, both fixed same session: (1) a Ciudad Juárez street ("Boulevard Juan Pablo II") leaked in because border.json's inTx() polygon test misreads a point 17cm from the line at the El Paso kink — fixed via border-zones.json's per-vertex zone labels, mirroring geo.js's classify(); first attempt broke Las Cruces/Mesilla coverage by omitting classify()'s documented neighborStates-first guard, reverted and fixed properly. (2) the min-stub-length crossing filter from the OK top-up wasn't the right invariant — FM 1218 (113u) and County Road 251 (47u) both run dead-straight along the NM survey line and sailed past any length cutoff; replaced with a distance-to-border-growth test (must gain 8+u over 30u of outward travel — real crossings measured 9-30u of growth, every parallel-runner under 5) | 1 full + several targeted / 0 flakes, but a real production-cost mistake: called the new Mexico-zone check unconditionally per-point in the bake loop, turning an 8-minute rebake into an 18+-minute one before catching it live (Bruno asked "are you stuck?", then "it is not 100% CPU" — it was, briefly misdiagnosed as fine before actually profiling and fixing the redundant per-point scan) | good, not high — the coverage/bug fixes are solid, but the runaway rebake cost real wall-clock time that should have been caught by reasoning about the added check's cost before running it broad, not after Bruno had to ask twice |
| 2026-07-16 | Band Parity W2 — life on the roads | Sonnet 5 | briefed control-city signs at every crossing + traffic on band roads + `bandTowns` tally, shipped as: (a) `buildGenericControlSigns` in shoulder.js, real per-state city table (LA/AR/OK/NM, ~30 cities via `LL()`), nearest-2 by straight-line distance, the 4 hand-authored signs kept as curated overrides — 102 crossings, 103 signs; (b) `TrafficSystem.polys` concats `GEO.highways`+`GEO.bandHighways` (no other change needed, already type-generic), plus an added `nearestBandCity` so real band towns get city-mix traffic instead of always-rural (scope addition, approved before coding); (c) `bandTowns` turned out to be a no-op — `save.passport.towns` already shipped this exact tally a day earlier in Shoulder & Shelf, spec corrected rather than duplicating it | 2 full + several targeted (shoulder/traffic/debug) / 2 own-check bugs fixed by reasoning (nCrossings vs signs off-by-a-hand-sign, then a nearestBandRoad long-segment index gap — switched the traffic check to GEO.bandHighways membership instead of patching the broken proxy); 2 unrelated flakes (ferries, then aviation+springer) cleared on isolated rerun, confirmed unrelated by diff scope | high — all three asks shipped; the W2c no-op saved a wave's worth of duplicate-mechanism work, and the nearestBandRoad gap was found and backlogged rather than papered over in the test |
| 2026-07-16 | Band Parity W3 — the ground | Fable 5 | briefed: retune the 0.75 out-of-state tan lerp + per-neighbor tints (LA swamp, AR pine, OK plains, NM desert) so DEM relief and cPine read; shipped exactly that — `neighborStateAt` (geo.js, bbox-gated ring test), `BAND_TINT`+`bandTint` (world.js, k≤0.5, Mexico keeps the 0.75 wash), 2 new band.mjs checks, 4 W3 tour spots. Plus the session's standing detour (Bruno-directed, pre-wave): Copilot CLI screenshot offload — images never enter Claude's context, tools locked down via `--available-tools` bogus-name trick, `tools/stage-shot.mjs` stager, workflow committed separately (2df15bb) | 2 full (run 1: 2 fails lost to output truncation, run 2 clean 396/396 — accepted per the documented under-`-j` flake class) + band/debug targeted; 11 staged shots + 10 Copilot calls vs 4 briefed shots — overage bought real diagnosis: OK tint needed brick-red input (olive base eats half the red), "black zigzag" = county lines + roads at grazing angle (not a defect), midground seam = global fog line (proven by Texas control shot), NM relief flatness = DEM cell resolution (backlogged as mountain-mesh candidate, not papered over) | high — the band finally looks like four states, one paint rule + a pure classifier, zero Claude-side vision tokens; shot overage was diagnosis, not thrash, and each extra shot answered a named question |
| 2026-07-17 | Band Parity W4 — crops and ranches | Sonnet 5 | briefed: extend the ag bake to band counties, teach `agAt` the band, swap `inTexas` gates in world.js placement (flora, crop decals, pivots, `farmsteadAt`, `chapelAt`+cemeteries) for band legality; prereq USDA extracts fetched live (295 MB national NASS dump, 4-state awk extraction, 249/249 county join clean after two normalizations — `St.`→`Saint`, plus a scoped alias for a pre-existing "DoC1a Ana" mojibake in neighbor-counties.json, root cause backlogged not fixed). Shipped exactly the briefed swap via new `inTexasOrBand`/`nearestAnyRoad`/`cityClear` primitives, plus two Bruno-approved scope adds: `feedlotAt` (same pattern as farmsteadAt) and a new tracked+styled `soybeans` crop (band-only — the biggest real coverage gap in the TX-only palette) | 1 full clean (404/404) + targeted (ag/band/shoulder/traffic/wildlife/debug) clean; 1 own regression caught pre-full-run by reasoning, not reruns — `animals.js`'s farm-herd code called the TX-only `agAt` on a newly-band-eligible `farmsteadAt` site and crashed on a null `.cattle` read; traced via an isolated repro, fixed by scoping the herd loop to `inTexas` on purpose (band herds are W5's explicit job, not a blind gate-widen) rather than patching around the crash | high — real census data end to end, zero fabricated coordinates (all 3 new Tours/check sites are a genuine, thematically apt find: Cotton County, OK growing cotton), and the animals.js crash was root-caused and correctly deferred to the wave that owns it instead of silently fixed out of scope |
| 2026-07-17 | Band Parity W5 — wildlife | Sonnet 5 | briefed: band region boxes in animals.js (swamp/pine/plains/desert, keyed on `neighborStateAt` — LA/AR/OK/NM, mirroring W3's ground tints), census herds wired to band farmsteads (fixing the W4-deferred `agAt`-on-null crash via the same `agAt\|\|bandAgAt` fallback world.js already uses), wander/flee clamp + road-avoidance widened `inTexas`→`inTexasOrBand`/`nearestRoad`→`nearestAnyRoad` throughout (`ranchHQAt` deliberately left `inTexas`-only, named real ranches). Shipped exactly as specced, no scope adds; new `bandWild()` debug action (one action, 4 tour spots, species follows `neighborStateAt` at the player) plus 1 deterministic Tillman County herd spot | 1 full clean (408/408) + targeted (wildlife/debug/ag) clean; 1 own check bug reasoned not rerun — the clamp check's first NM coordinate was a DRIVE tour spot sitting ON a real road (Hobbs via NM 18), so `nearestAnyRoad` blocked every direction and read as a broken clamp; fixed by walking the test's spawn point off-road first, not by weakening the assertion | high — band land now has its own fauna instead of leftover Texas-box coincidence, farmstead herds finally render there, and the only detour was a 5-minute test-coordinate fix, cleanly diagnosed |
| 2026-07-17 | Band Parity W6 — band fields + track close | Sonnet 5 | briefed: 2-4 real band airports (traffic + charter reach) then track close. Found before coding: the airport ask was already fully satisfied by Shoulder & Shelf's 6 band fields (incl. Texarkana, full ROUTES/traffic/charter wiring), and the spec's other named exemplars (Roswell/Lawton/Lake Charles) are law-excluded from the band as glow-only vignettes just past the 25-mile line — adding them would have broken a standing `band.mjs` assertion. Flagged to Bruno pre-code; he chose no-op + a backlog item over researching substitute fields or overriding the exclusion. Shipped: `ROADMAP.md` 6-wave fold, `GOTCHAS.md` W5 amendment + rebake-check bullet amendment + a new exclusion-law bullet (so this exact spec-vs-check conflict isn't rediscovered), `BACKLOG.md` header + new deferred-airports item, 3 stale `MODULES.md` one-liners corrected (airports.js still said 21 fields/7-9-5 from before Shoulder & Shelf; animals.js/world.js didn't mention the band additions), `CLAUDE.md` active-track line caught up two tracks, `NEXT_SESSION.md` briefing deleted | 1 full clean (408/408), 0 flakes; zero targeted runs (no runtime code touched) | high — caught a contradiction between the spec and a standing law before writing any code or fetching any data, and the docs sweep found three unrelated stale facts left over from an earlier track that a straight airport-adding session would never have surfaced |
| 2026-07-17 | New Player W1 — boot plumbing | Sonnet 5 | briefed: title screen skeleton (Continue/New game), resume at last position/heading/mode/altitude/clock (`save.at`), save & quit to title, harness bypass. Freeze-mechanism reading was genuinely open (spec's "pre-loop" could mean render-loop-deferred or sim-frozen-behind-a-live-loop) — surfaced to Bruno as an explicit A/B choice before coding, via advisor + AskUserQuestion; he picked B (reload-based quit, render loop starts only after title dismissal — the literal reading, no freeze flag). Shipped exactly that: `gameplay.js` `snapshotAt`/`applyAt`, `title.js` (new, always-built logic per the debug.js pattern), `index.html` `#title` + paused-quit button, `main.js` harness branch with zero new awaits on that path, `debug.js` forcing Actions-tab button (no Tours entry — no world coords), `tools/verify.mjs` `window.__harness`, 5-check `onboarding.mjs` | 1 full clean (415/415) + fast `test.mjs` (4/4); 1 own-check bug reasoned not rerun — a `t.tp` FLY-mode settle drift (0.15s sim wait) made a hardcoded-constant assertion flaky, fixed by asserting against the settled pose read back live instead of the raw teleport args | high — all four W1 asks shipped on the literal spec reading, the open design fork was surfaced and resolved before any code was written, and the only bug was in the test's own assumption, caught and fixed pre-full-run |
| 2026-07-17 | New Player W2 — first-run experience | Fable 5 | briefed: concept card (Start / Skip intro & tips), 3-4 staged tutorial toasts, curated new-game spot, title dressing (attract drift + rotating fact), one staged shot. Shipped all four plus two unbriefed finds: the budgeted Copilot shot caught the HUD (minimap/stats/speed) leaking around the newly-translucent title — fixed via `body.title-up` visibility rule, asserted numerically instead of a second shot; and the loop restructure (attract branch replacing W1's blocking pre-loop await, the carried-over gotcha, resolved as planned in the implementation plan) incidentally unblocked `tools/stage-shot.mjs`, which never sets `__harness` and would have hung on W1's await. Curated spot interpolated onto the real I-35 New Braunfels approach polyline (985, 1737, heading 1.582); facts pooled from LANDMARKS + SPECIES + two census templates (no authored ag pool exists — spec assumed one); `save.seen` additive key with veteran grandfathering; `firstRun` debug action + 2 tour spots | 1 full clean (420/420), 0 flakes; onboarding suite green first try both runs | high — every briefed item shipped on budget, and the single shot earned its slot by catching a real shipping defect the numeric checks could not see |
| 2026-07-17 | New Player W3 — hints, help, Guide, Settings | Fable 5 | briefed: 5 first-encounter hints via `save.seen`, help sectioned Driving/Flying/Menus/Goals, Guide replaying card + tips + hints, labeled Settings panel on pause + title, checks in onboarding.mjs, ≤3 shots (first wave under the new shot policy). Shipped all four plus: hintCity absorbs tipMap (Bruno's pre-code pick from an AskUserQuestion — kills a double map lesson), hintBand outranks hintAirport (probe found Hobbs sits on an airfield footprint AND has townsfolk in talk range — the crossing hint was losing its own moment), `stage-shot.mjs` gained `--eval` + `__harness` auto-enter (it booted into the W1 title screen since that shipped — every world shot was silently title-blocked), `title.onShow` refresh hook, stale-signal clear on the hintSig guard. New `settings.js` module, `hintsReset` debug action, 6 tour spots | 1 full clean (426/426); first targeted run: 1 boot-break (TDZ — `hintSig` referenced by `__game` before declaration, caught by a scratch pageerror probe) + 3 check bugs all reasoned in one probe pass, not rerun-hunted (tipCollect leaking into a hint count via W2's pushed fixtures; Hobbs triple-signal collision; `toggleMute` no-op without AudioContext — stubbed the two members it touches); second targeted run + full run clean | high — the game now teaches itself at the moment each feature first appears, everything taught stays re-readable, five invisible keybinds became a real panel; the probe-first flake discipline held (zero brute reruns) and the shot budget caught nothing this wave but cost three stagings |
| 2026-07-17 | New Player W4 — named save slots + track close | Sonnet 5 | briefed: 3 named slots on the title screen (rename/delete confirm), save + the 4 comfort keys per-slot, legacy migration to slot 1, isolation/settings/migration checks. Export/import dropped to `BACKLOG.md` before coding (Bruno's call, per the spec's own pre-authorization). Advisor call before coding settled the one real design fork: live in-place slot switching, never a page reload — the hard requirement puts `select`/`newGame`/`rename`/`delete` on `__game.title` specifically because the harness's context wipes localStorage on navigation, so a reload-based switch would be untestable. That forced a wider blast radius than the spec assumed, found by reading before coding rather than after: `missions.js` cached a stale `save` reference (fixed to a live getter) and `shop.js`'s `applyGear`/cargo-mesh state weren't re-applied on a switch — both would have leaked one slot's progress into another. New `src/slots.js` (key scheme + module-load migration, ordering-sensitive against brands.js's module-level scale read); `gameplay.loadSlot()` disposes+rebuilds the mesh-backed visuals (city/band stars, roses, landmarks) since they bake save state at construction and only ever remove during play; dynamic 3-row title UI. Shipped the full track close: `ROADMAP.md` fold, `GOTCHAS.md` graduation (slot scheme, live-switch pattern, the always-built/presentation-gated UI law, a Copilot-read-vs-DOM-numbers lesson), `NEXT_SESSION.md` briefing deleted, `CLAUDE.md`/`BACKLOG.md` swept | 3 full runs: 1st surfaced 7 fails, all from *other* suites (brands/haunts/hud/missions/shop) still reading the pre-slot bare save/settings keys directly — fixed by suffixing `:1`, not a regression in new code; a `this.save.x ??=` static-source test also needed its regex updated for the same refactor; 2nd and 3rd clean (433/433); onboarding run standalone 5+ times while landing a real bug (an array-of-materials mesh — rancharch's sign plate — broke the naive `material?.map?.dispose()` optional-chain, since `Array.prototype.map` is truthy; fixed by flattening to an array unconditionally) and a one-off concurrency-timing flake on an unrelated pre-existing check, confirmed clean 3× standalone before trusting it | high — three isolated, renameable saves with independent settings, and the two silent cross-slot leaks (missions bank/job, shop perks) were caught by reading the actual consumers before writing the switch path, not by a player report later; one screenshot judgment call (row-height consistency) was overridden by a direct DOM measurement after Copilot misread it twice, keeping "assert numbers, not pixels" honest even against the tool built to save Claude's context |
| 2026-07-17 | Energy spec session | Fable 5 | Bruno's energy-dimension directive → track opened: ENERGY_SPEC.md, 6 waves (bake / extraction / wind+solar / refining / grid+heroes / jobs + close). Every scoping number live-probed via Overpass counts before waves were sized (76,654 wells, 27,644 turbines, 1,422 plants — 1,131 named, 22 refineries, 397 offshore platforms, 42,881 line ways, 9,427 substations); 3 open calls resolved via options (Energy log as 11th collectible / jobs in-track as W6 / 345 kV spine only) + 4 same-session follow-on directives folded in: W6b poly bar graduated to a GOTCHAS standing law, real-light pool (fixed-size, sky.js-owned) into W4, realism-first replacement — old scatter + hand-laid platforms retire, bake-measured scarcity exception (ships trigger it: 0 OSM lanes, 8 fairways; platforms don't: 397 real sites) — and the approach announcer (real names on the HUD, unnamed silent). W1 briefing written | 0 runs (doc-only) / — | good — the spec's every scale decision rests on a measured count, not an estimate; the one layer where realism was impossible (ship routes) was proven impossible before the exception was written |
| 2026-07-17 | Energy W1 — data bake | Sonnet 5 | briefed: fetch + `build-energy.mjs` + `data/energy.json` + `geo.js` wiring + `energy.mjs` checks + save-reference grep, no visible change. Shipped exactly that; every raw layer count landed exactly on the spec's live-probed figures (76,654 wells / 27,644 turbines / 1,422 plants / 22 refineries / 397 platforms / 8 fairways). Two invisible bugs caught before they could ship a hero-site gap, both 200-OK-but-wrong: `overpass-api.de` 406s on a mixed way+relation UNION query (a new failure mode beyond the known POST-406), and the mail.ru mirror silently omits relation `members`/geometry under `out geom`/`out tags` — together these would have dropped all 483 relation-typed power plants, including the W5 hero Comanche Peak. Fixed by fetching ways and relations as separate single-type queries and resolving relation centroids from recursed member geometry (`out body;>;out geom;`, node- and way-member aware). Also rewrote the 345kV line stitcher bidirectional — `build-rails.mjs`'s forward-only chaining (grid segments don't share rail's consistent way-direction convention) left 75% of ways unmerged — and dropped 1,633 sub-500m substation-yard jumper segments (89% sampled within 300u of a baked substation) as noise, cutting `lines345` from 264.8KB to 155.6KB. Final `data/energy.json` is 354KB against the spec's "≲150KB" aspiration — missed, in line with sibling boot files (rivers.json 297KB), driven by the spec's own requirement to keep 1,422 named plants individual for the W2 announcer | 1 full clean (445/445), 0 flakes; `energy` suite (9 checks) green first try; no test reruns needed, all debugging was fetch/bake-side against raw JSON, not the browser harness | high — both invisible fetch bugs would have shipped a data-correctness defect straight into a named W5 landmark with no failing check to catch it (my first `energy.mjs` pass would have asserted against the *already-broken* bake and passed); caught by inspecting raw counts against the spec's pre-verified figures before writing checks, not after |
| 2026-07-17 | Energy W2 — extraction real, offshore rebase, log + announcer | Fable 5 | briefed: `wellSiteAt` + pump/tank-battery/rig kit, uniform Permian scatter retired, offshore rebase onto the 227 baked platforms with Far Rig + skyline sentinel re-anchored, night gas flares, Energy log (11th collectible, Spindletop + Midland Tank Farm), approach announcer (armed/re-arm, name→operator→silent), one staged shot, pre-authorized W2.5 split unused. Shipped all of it in one session: well pads seeded per county density (array-returning `wellSiteAt`, up to 3/chunk, full legality chain), per-component InstancedMesh platform fleet (~2000 draw calls avoided), fairway-snapped port-approach legs + a working approach tanker (spec line honored, not skipped), announcer + log machinery in new `energy.js` with later waves reduced to `register()` calls. One design deviation surfaced by a failing sentinel, not by a player: the true farthest major (Gunnison Spar, 153.6 mi) sits past the 1127u world wall — Far Rig re-anchors to the farthest *reachable* (`inWorld`) major (Peregrine Oil & Gas, 61.9 mi); the shelf check now asserts reachability so dead brass can't ship | 1 full clean (452/452), 0 flakes; energy suite grew 9→16; 1 boot break caught by scratch probe (fairwayLegs built after buildShips consumed them); shot budget: 1 briefed, 4 stagings — Copilot flagged the flare flame floating over an invisible night stack twice, fixed with a basic-material lit tip segment | high — the basins now read where the wells really are (Eagle Ford/Barnett/East Texas appear from nothing, zero-well counties empty out), the Gulf holds its real 227-platform fleet, and every named site introduces itself; the extra 3 stagings were the shot policy doing its job on a genuinely visual defect |
| 2026-07-18 | Energy W3 — wind farms, solar fields, log heroes | Sonnet 5 | briefed: instanced turbines density-scattered within the baked `windFarms[]` circles (dual InstancedMesh — static tower/nacelle + a per-frame blade-matrix rebuild joining the existing windmill real-loop sentinel, no second ticking system), solar panel fields (`mkFieldPatch` decal + `mkCropRows` near-ground rows, zero new geometry makers), 3 wind log heroes (Roscoe, Horse Hollow, Papalote Creek — anchored inside their real baked farm clusters, road clearance pre-verified offline before coding) registered into the W2 announcer with no new toast machinery, one shot (turbine row at dusk). Shipped all of it; the shot caught a real pre-commit defect — the literal-scale turbine geometry read as bare toothpicks/scattered noise at normal viewing distance — fixed by bulking tower/nacelle/blade dimensions ~2x past real proportions (same legibility-over-realism call as the poly-bar rule) and reverified clean at both aerial and ground distance | 3 full clean runs (457/457 final), 0 flakes; one incidental test-baseline update, not a regression — `band.mjs`'s frozen LBJ-ranch-chunk pin absorbed +1 `solarfield` because a real baked solar site legitimately shares that chunk | high — wind and solar now read as real farms at their true Texas locations and density, not decoration; shot budget ran 9 stagings against 1 briefed, but the overrun bought a genuine geometry fix caught before commit, not framing vanity — camera-crop iterations were abandoned once the fix was confirmed rather than chased to a perfect frame |
| 2026-07-18 | Energy W4 — refineries, hero skylines, light pool, spill decals | Fable 5 | briefed: refinery kit at "all 22" + 4 hero skylines + local light pool + spill decals (rigs included) + plaques + log sites, two shots. Shipped all of it plus an unbriefed-but-necessary data fix surfaced before coding: the shipped refineries[] (22) mirrored a too-narrow OSM query that missed the entire Houston Ship Channel cluster — two of the four promised heroes (Baytown, Ship Channel) had no baked site. Bruno approved rebake (broadened landuse+name query recovering Deer Park/Big Spring/Borger/McKee/Corpus East, junk filter, 6u near-dup collapse) + 2 hand-placed OSM-unmapped majors (ExxonMobil Baytown, Marathon Galveston Bay/Texas City — scarcity exception) → 33 baked / 28 named. Kit: 33 sites → 4 merged meshes, per-prop road/river clearance scaled by prop extent with seeded retry placement (per-prop application of the Blue Wing footprint law, enforced by a sampled vertex sweep check); heroes ×1.7 with cracker/tank-farm/twin-flare dressing + brass plaques (5th plaqueNear source) + 4 log entries. Light pool: 6 PointLights born at boot, kind-colored, 0.5 s nearest-anchor assignment, flare flicker; anchors from refinery flares/columns + 152 major rig decks + Far Rig. Spill: vertex-color radial-falloff additive discs (fadeDisc), draped on ground / floated over the gulf plane | 1 full clean (467/467 under -j4), 0 flakes; energy suite 22→31; first W4 check run: 4 fails, all reasoned in one pass (2 real code gaps — extent-blind clearance, no retry for road-hemmed sites; 2 wrong check bounds) and green on the next run; shot budget 2 briefed / 7 stagings — overrun bought two real pre-commit fixes (flame spheres unreadably small at play distance, the W3 turbine lesson repeating on flames; hard-edged spill discs) plus aim iterations on the Ship Channel frame | high — the promised hero wave was unbuildable against the shipped data; the rebake made "Ship Channel at night" a real destination instead of two absent heroes, and the light pool is the reusable W5/W6 payoff |
| 2026-07-18 | Energy W4.5 — solar rework (Bruno-prompted detour) | Fable 5 | prompted by Bruno's question ("why rounded? panels with base + incline?"), scoped and approved before coding: tilted south-facing panel prototype (slab + legs, merged, instanced, never rotated — the aerial read) replacing flat crop-row boxes, crop-circle decal replaced by a 2x2 grid of rectangular dirt-pad blocks with PER-BLOCK road/river clearance (skip, never shrink — Blue Wing now legitimately keeps its road-clear blocks instead of vanishing), panel/pad contrast fix (panels were near-identical hue to the decal beneath — the real reason every read said "flat paint"); real-polygon rebake evaluated and rejected (547 polygons into an oversized boot file, poor ROI) | full 468/468 after one legitimate frozen-baseline absorb (band.mjs LBJ chunk +1 solarfield — the site W3's whole-site clamp erased now draws its clear blocks); energy suite 31→32 (per-block law mirror + panel-geometry facts); 6 stagings against an implied 1 — 2 real fixes (leg gap, hue collision) then iteration stopped per the W3 rule, aesthetics handed to Bruno's eye | good — the fields now read as engineered solar farms from air and ground for the cost of a mini-session; the hue-collision find is the reusable lesson (contrast, not geometry, was the invisible-detail culprit) |
| 2026-07-18 | Energy W5 — 345 kV tower corridors, substations, hero plants, ERCOT radio | Sonnet 5 | briefed spine (tower corridors + major substations + 4 hero plants + ERCOT radio flavor), one shot (tower corridor read). Shipped all of it: ~7,400 box-built lattice towers instanced by arc-length across the 945 baked corridors (trains.js `arcInit` idiom, spacing tuned for legibility, not real 300-500m spacing) plus a conductor ribbon carrying the eye-line; 735 baked substations runtime-thinned to ~608, one vertex-color-baked InstancedMesh kit, named+separated sites join the announcer (Parish's own substation correctly excluded — 10.6u from its hero, no double toast); 4 new hero plants (South Texas Project + Comanche Peak nuclear, W.A. Parish, Martin Lake) with facts verified by web search, all baked coords already road-clear (24.9-148u, no Spindletop-style shove needed) — existing generic hero machinery (announcer/log/plaque/clearance sweep) absorbed them with zero new code. ERCOT radio flavor landed as a `{grid}` context token (the `nm`/`shelf` located-wink idiom) added to the existing `ga`/`jet` chatter pools rather than a new chatter kind — sidesteps the carried-over "3-rows-or-it-never-speaks" trap by not needing a new kind at all. One scope call surfaced before coding and put to Bruno: the spec's 5th W5 candidate (ERCOT control room, Taylor) dropped in favor of radio-only representation — a nondescript office building was a weak landmark payoff | 2 full clean runs (476/476 final) + 1 named-suite-only run mid-session; one `springer` flake under `-j4` (NPC-hint-priority race, unrelated system), confirmed clean standalone before ruling it pre-existing | high — the corridor needed one legibility correction (thin lattice members collapsed to bare poles past ~2 tower-lengths; Copilot's first shot caught it, thickened members + darker steel fixed near/mid field, the conductor ribbon + repetition still carry the followable-line read at range) before the promised result held up; everything else — substations, heroes, radio — landed first-try clean against pre-verified facts and offline-probed coordinates |
| 2026-07-18 | Energy W6 — energy jobs + track close | Fable 5 | briefed: crude/fuel/oversize-blade job types + oversize speed-over-time rule + balance + track close, no shots. Shipped as briefed: 3 energy offers on the board (8→11), endpoints reference hero ids resolved at use (orphan self-clears through the existing target() machinery — zero new plumbing forked), oversize ×1.5 tracked per frame with a HUD 🐢 tag that flips to "bonus lost" + a loss toast at the moment of the burst, pure terms/verdict in mission-rules.js under tools/test.mjs rules, pinned forceEnergy debug hook + 3 auto-loading Tours spots; fast-travel lock inherited from the phase gate; no new announcer/log machinery (spec held). Track closed: ROADMAP fold-in, 5 gotchas graduated (voltage multi-value, farm clustering, flare gating, hero-id save keys, speed-over-time rule), BACKLOG/CLAUDE headers swept, briefing deleted | 1 targeted batch + 1 full (482/483) / 1 flake (aviation A3 chatter roll — pre-existing probabilistic class per the GOTCHAS rerun policy, clean standalone); every new check green first try | high — a whole job dimension plus the track's paperwork in one clean session; designing the verification shape (burst-never-forgotten) before the code is why nothing iterated |
| 2026-07-18 | West Texas massifs W1 — the Guadalupe wall (one-off wave, no track) | Fable 5 | briefed: hero ridge meshes over real terrain (TX escarpment + NM Brokeoff + reef arm), Guadalupe Peak summit landmark (new `summit` kind), 3 tour spots, new massif suite + padre/unit-test count bumps, one shot, grep-first. Shipped that, then Bruno's pre-commit playtest caught three defects the first pass missed: floating historical markers (a GENERIC bug — every landmark's plaque sat at center-height 5.5u off-center, airborne on any sloped site; fixed in mkLandmarks with per-marker hAt re-drape), tent skirts floating on slopes (base now sinks to the footprint ring's lowest terrain sample), and too-smooth geometry (merged craggy cones 10×4 + jitter + 2 seeded satellite knobs per anchor with saddle/road legality, replacing the 4-seg instanced pyramids). Final: 14 spine tents + ~26 knobs, 39th landmark, 6-check `massif.mjs` | 2 full cleans (487 then 489 under -j4), 0 flakes; 5 stagings for 1 shot slot (1 mis-typed staging — own error; 2 legitimate look iterations: amplitude alone didn't read, satellite knobs did) | good — the far-west skyline plus a generic floating-plaque fix for a bit over a half-session; lessons: the backlog premise was stale (`buildMountains` was dead fallback code) and the first screenshot question was too narrow — it asked "is there a wall" and missed smoothness and float |
| 2026-07-18 | Performance W1 — instrumentation + baseline (PERFORMANCE_SPEC.md, spec written same session) | Fable 5 | briefed: spec (vision folded in as preamble) + perf lap timing for every render-loop system + debug Perf tab + 3 staged tour spots + perf suite, no shots, grep-first. Shipped as briefed: `src/perf.js` PerfMonitor (EMA avg/max/tick-count per system, zero steady-state allocation, `renderProbe` for true draw counts under `__skipRender`), ~35 lap points in main.js, 📈 Perf tab (2 Hz snapshot, reset-max), Performance tour track (Houston night storm worst case / I-10 west floor / Sweetwater dense ambient), 6-check `perf.mjs`; harness discovery baked into design: the fake clock zeroes `performance.now`, so checks assert structure + tick counts, real ms come from the browser. Baseline table pending Bruno's machine read | 1 targeted + 1 full (495/495), 0 flakes; one real find pre-full-run (fake clock exposes `performance.memory` with no fields → NaN, gate on the number) | good — the game can now say where its frame goes; W2 thresholds and all future optimization hang off this readout |
| 2026-07-18 | Performance W2 — verify guardrails | Sonnet 5 | briefed: count-based draw-call/triangle ceilings (baseline + spec headroom, 2500/2.5M) checked via `renderProbe()` at the 3 W1 baseline tour spots, plus a per-track perf-cost line in CLAUDE.md's multi-wave protocol. No per-system/total ms thresholds (spec's Finding 5 ruled those out: machine-bound, all tiny). Shipped exactly as specced, no deviations, no gameplay change | 1 targeted (perf) + 1 full (497/497), 0 flakes | good — cheap regression tripwire (32 lines) landed ahead of W3 touching the actual draw-call architecture, exactly the spec's measure-guardrail-optimize order |
| 2026-07-18 | Performance W3 — draw audit + fog-wall gate | Fable 5 | briefed: differential draw-call audit by source (diagnosis, fix only if the data pointed at one clear low-risk cause), findings into the spec, no shots, grep-first. Shipped the audit (`perf.drawAudit()` differential probing + per-kind scenery split via universal `userData.kind` tags, 🔎 Audit button, exact-sum suite check) — and it REFUTED the briefed suspect: scenery was uniform (~380–650 calls); the real base was world-spanning boot decoration fog hides but never culls (shoulder vignettes 566–611 calls everywhere, landmarks + city stars ~350–500; camera far 30000 vs fog wall 1400). Same-session fix per the briefed contingency: `FogGate` (sky.js) on shoulder + gameplay decor, fog:false auto-exempt. Measured: Houston storm 2041→934, desert 1579→675, Sweetwater 1510→866, solar field 1493→633 — draw base halved everywhere. Caps retuned 2500→1600 against the harness's post-teleport context (Finding 8: teleport probes read ~+300); band frozen baseline re-pinned (tags widened its count, placement untouched) | 2 full (496→499), 0 flakes; 3 suite-fail iterations, each reasoned not rerun (live-loop race in the restoration invariant → moved in-block; cap vs teleport transient → measured both contexts; band baseline semantics → re-pinned stronger) | high — the wave's question ("why does empty desert out-draw Houston?") got a data-backed answer that overturned the suspicion a refactor would have chased, and the fix that fell out halved draw submission for ~90 lines |
| 2026-07-18 | Performance W4 — real-hardware re-record + track close | Sonnet 5 | briefed: re-record the W1 protocol at the 3 tour spots on Bruno's machine to confirm the render-ms drop, fold the track into ROADMAP.md, sweep BACKLOG/GOTCHAS, delete the NEXT_SESSION briefing. Shipped exactly that — real hardware confirms the fix: render avg −20% to −54% across all three spots (I-10 floor hit hardest at −54%, matching W3's diagnosis that its `shoulder` share of the pre-fix total was the largest of the three), Sweetwater now holds a steady 60 fps (was 57.5). One unchased outlier noted in the spec, not investigated: Houston's 224 ms max frame, ~118 ms of which sat outside every instrumented lap (a stall, not game work) | 0 runs — doc-only diff, no code touched, verify not required | good — the W3 fix holds up off the harness; track closes clean, 4 gotchas graduated |
| 2026-07-18 | Railroads spec session | Fable 5 | briefed: read the two vision docs, write RAILS_SPEC.md + W1 briefing, resolve 4 open calls; shipped exactly that — all 4 calls resolved via options (maps-only presence / seeded schedule / band rails as W3 / commuter passenger sets), 3-wave split (W1 Fable regrade on the look-risk rule, W3 Sonnet), design-settled sections incl. livery hexes + seed streams + force-hook contract; recon found the vision stale (🚂 placard already shipped 2026-07-15, umbrella's W2 collapsed into W1's map item) and confirmed baked track reaches both border gateways (CPKC Laredo Sub 6u, UP Eagle Pass Sub 5u); BACKLOG riders annotated, CLAUDE.md active-track line updated | 0 targeted + 0 full (docs-only) / — | good — the stale-vision catch deleted most of a planned wave before it was ever briefed; 2-wave estimate became 3 only by folding the band-rails backlog item in |
| 2026-07-18 | Rails W1 — operator surfacing | Fable 5 | briefed: operator liveries + commuter passenger sets + rails on maps + force hook + new rails.mjs + one palette shot; shipped all, with two data-driven corrections surfaced to Bruno before coding on: DART dropped from the commuter set (light rail, real-or-absent) and commuter spawn thresholds relaxed after recon found the baked TRE/TEXRail corridors shredded (17 pieces, none passing the mainline filter) — Bruno chose fragments-now with the bake-join fix as a W2 rider; livery hexes retuned once off the Copilot judgment read (spec hexes muddy under Lambert noon light) and re-validated | 1 full clean (504/504) + 1 targeted batch (rails/lights/debug/hud) all green first run / 0 flakes | high — the whole surfacing wave (liveries, passenger sets, maps, deterministic forcing) landed in one session on the heels of the spec, and the lights suite's 45 s spawn wait is retired; shot budget overran (6 staged frames for 2 surfaces) on staging bugs, each diagnosed before re-staging, not design churn |
| 2026-07-19 | Rails W2 — the border show | Fable 5 | briefed: spur bake + commuter defrag rider, seeded railxing: schedules, 3 named trains, bridges, forcing actions + tours, suite extension, one crossing shot; shipped all — defrag gate passed byte-identical on a fresh fetch before the fix (TRE 17→8 pieces, 164→496 u), plus two playtest-driven fixes the same session: junction hop (named trains roll onto the connecting sub instead of braking at end-of-line — Bruno caught the Laredo dead stop) and the Eagle Pass ping-pong stall (min-onward-run hop guard + a 1 km gap-bridging bake pass + 15 u hop radius; Manifest now runs 5800+ u border→Glidden Sub). Schedule also hardened: an open window never teleports a live watched train | 3 full (510/512/513) all green + targeted rails/debug/lights batches / 1 real failure (beam array sized to the old loco pool), 0 flakes | high — the border show shipped and survived Bruno's playtest same-session; shot budget overran again (4 frames for 1 surface: 1 real defect caught — segment-angle bridge clipping the curve — then FLY-avatar + framing staging rounds) |

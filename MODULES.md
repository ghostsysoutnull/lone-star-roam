# Where things live — grep anchors per module

One line per module: key exports / methods / knob sites by *name* (grep these;
line numbers rot, names don't). Architecture & gotchas stay in CLAUDE.md.

## src/

- `geo.js` — `GEO` singleton, `hAt` (terrain height, `ELEV` grid consts), `nearestRoad` (spatial grid, optional type filter), `nearestCity`, `inTexas`, `countyAt`, `agAt` (`countyAt` → `GEO.ag` record, null outside Texas), `waterAt`, `TIDELANDS_U`/`coastDist` (island-aware distance to the Texas coast)/`inStateWater`, `neighborDist(key,x,z)` (distance to an `LA`/`AR`/`OK`/`NM` ring, 0 inside — the chatter engine's west gate), `seededRand` (never change seed strings).
- `world.js` — `buildWorld` → `buildTerrain`/`buildHighways`/`buildWater` (gulf plane vertex-colored by `coastDist`: teal state water → blue past the Tidelands line, one mesh)/`buildCountyLines`/`buildRibbons` (all roads = merged meshes per tier); `ScenerySystem` inside `buildWorld` return (chunked flora/props, `group.userData.animated`, flora scatter loop now `inTexasOrBand`-gated); `bandTint(x,z)` (Band Parity W3: per-neighbor `BAND_TINT` — LA swamp/AR pine/OK plains/NM desert — off `neighborStateAt`, replacing the flat out-of-state desert wash; Mexico keeps the old `cOut` wash); `chapelAt`/`chapelSitesNear` (pure seeded chapel+cemetery sites, `mkChapel`/`mkCemetery`, `inTexasOrBand`-gated); ag layer: `CROP_STYLE` (per-dominant-crop decal/row styles, band-painted from `GEO.bandAg`), `mkFieldPatch` (hAt-draped decal, `round` for pivots), `mkCropRows` (per-patch InstancedMesh), `farmsteadAt` (pure seeded farm sites, odds straight from `agAt || bandAgAt` census, `nearestAnyRoad`/`cityClear` legality), `feedlotAt` (same pattern, onFeed-density ≥30 gate = top 9 counties by real data — dormant in the band, real OK census never crosses the gate — returns pen world centers shared with animals.js), farm kit `mkBarn`/`mkFarmhouse`/`mkStockTank`/`mkCorral`/`mkSilo`/`mkChicken` (pecks via animate-loop kind `'chicken'`), feedlot kit `mkFeedPen`/`mkFeedMill`; Energy W2: `wellSiteAt(cx,cz)` (pure seeded well pads — returns an ARRAY, up to 3/chunk, odds from `energyAt().wellKm2`; the old uniform Permian pumpjack table entry retired, stream unused not re-keyed) dressed with `mkWellPad`/`mkTankBattery`/`mkDerrick`/`mkFlareStack` (+ reused `mkPumpjack`), `ScenerySystem.flareMat` (shared flame mat, opacity = `ATMOS.night`, per-flame `'gasflare'` scale flicker); `mkSolarPanels` (W4.5: merged tilted-slab+legs prototype instanced south-facing, never rotated; solar sites draw as a 2x2 grid of rectangular dirt-pad blocks with PER-BLOCK road/river clearance — skip, never shrink), `RANCH_HQ`/`ranchHQSite(i)`/`ranchHQAt(cx,cz)` (waves 5/5b: compounds behind all EIGHT gate arches — King/6666/Waggoner/Y.O. + JA/XIT/Matador/LBJ — seeded lawful site + 3 pen centers, coords sync with gameplay/animals, `inTexas`-only by design) dressed with `mkHQHouse`/`mkWaterTower(sig)` (per-ranch sign, cached mats)/`mkHorseBarn` (6666)/`mkFlagpole` (LBJ) + Waggoner in-compound pumpjacks + XIT windmill row, props tagged `userData.prop`.
- `vehicle.js` — `Player`: `update` (physics branches; DRIVE `caps{}`, offroad/rain via `this.perks`), `animate` (lights/particles; headlight intensity/lead knobs), `setMode`, `resetToRoad`; `mkTruck` (`userData`: headlights/wheels/brakes/cargo/beams/bodyMat), `mkWings` (`userData`: mat/stockColor — missions.js charter livery swap), `mkCowboy`, `mkStarMesh`.
- `sky.js` — `ATMOS` (mutable: wind/night/weather/rain/ufo), `SkySystem.update` (weather state machine: `nextPick`, `forecast`/`forecastT` radio window, `blend`; time keyframes `KEYS`), `WEATHER`/`ODDS` tables, `forecastLine`, `weatherIcon`, `skyReport`, `buildCelestial`, `DAY_SECONDS`; Energy W4 local light pool: `POOL_N`/`POOL_KINDS` (6 PointLights built once at boot — count constant forever, shader-recompile law), `registerGlowAnchor({x,z,y,kind})`, 0.5 s nearest-anchor assignment + per-frame `ATMOS.night` intensity with `flare`-kind flicker in `update`.
- `gameplay.js` — `Gameplay`: save load/`persist` (per-slot key via slots.js; keys: cities/landmarks/roses/species/stats/counties/ufo/bank/jobsDone/job/gear/legends/airports/at/seen/name), `LANDMARKS` (+`LL` projection; `rancharch` gate arches — coords sync with animals.js `RANCH_ARCHES`, King gets wings+windmill dressing in `mkLandmarkMesh(kind, name)`), `enterCounty`, `spotSpecies`, `spotLegend`, `ufoSighting`, `logAirport` (10th collectible, dedup by field id), `logEnergy` (11th collectible, `save.energy` hero-site ids, energy.js drives it), `landmarkNear`, `mkRoses` (rose RNG — never touch), `snapshotAt`/`applyAt` (`save.at` resume — position/heading/mode/altitude/clock, title.js Continue), `loadSlot(slot)`/`_disposeVisuals` (New Player W4 — reloads `save` + rebuilds the mesh-backed visuals for a different slot).
- `title.js` — `TitleScreen`: boot title screen over the live attract drift, always built (harness auto-enters via `window.__harness`, never shown); `hasSave`/`summary`, `show`/`hide` (toggles `body.title-up` HUD hide + `active` for main.js's loop branch), `attract(dt, camera)` (orbit drift + fact rotation), `buildFacts` (landmark/species facts + census templates), intro card (`needsIntro`, `finishIntro(skip)` — Start vs Skip intro & tips, `save.seen`), `onEnter` (main.js: welcome + tutorial arm), `apply(choice)` (`'continue'`→`gameplay.applyAt`, `'new'`→ the SA_START spawn callback) — the seam `tools/checks/onboarding.mjs` drives directly. New Player W4: `slots()` (row data for all 3), `select`/`newGame`/`rename`/`delete` (live in-place slot switching, no reload — the seam the suite drives), `_afterLoad` (re-applies the 4 comfort settings + `shop.applyGear` + `missions.crate` after any switch), `renderSlots` (dynamic `.slots` DOM — presentation only).
- `slots.js` — save-slot storage scheme (New Player W4): `KEYS` (every base localStorage name — never hardcode a `'lonestar-…'` string elsewhere), `SLOT_COUNT`, `activeSlot`/`setActiveSlot` (`lonestar-slot`, the only global key), `slotKey(base, slot=activeSlot())`, `readSlotSummary`/`deleteSlot` (storage-only, no live Gameplay needed), `migrateLegacy` (also runs once at module-load time — must precede brands.js's module-level scale read).
- `onboarding.js` — `Tutorial`: staged first-run tips (`TIPS` priority table: tipV/tipMap/tipCollect/tipHelp) + contextual first-encounter hints (`HINTS`: hintNpc/hintCity/hintDusk/hintBand/hintAirport, event signals from main.js's `hintSig`, hintCity absorbs tipMap via `also`), fired once per save via `save.seen` keys, `seen.all` silences all (card Skip or pause-screen skip); `begin`/`skip`/`pending`/`update(dt, sig)`, `fired` order array for the suite; `buildGuide()` fills the help panel's read-only Guide (intro-card clone + both tables).
- `settings.js` — `initSettings({audio, hud, missions, brands})`: the five keybind settings as a labeled panel (`mount(container)` on #paused + #title, `refresh()` re-reads live state); storage-agnostic — each control calls the keybind's own function (toggleMute/uiScale/toggleCompass/toggleArrow/setScale).
- `missions.js` — `MissionSystem`: `genOffers` combines `genGroundOffers`/`genCharterOffers` (pay/deadline knobs at top, `CARGO`/`MANIFEST`/`REAL_ROUTES` tables, exported as `POOLS`; CARGO `from`/`to` are preferred endpoints that MUST resolve in `GEO.cities`, `note` is the only place flavor names the world across the line), `accept`/`load`/`deliver`/`finishJob` (payout multipliers, kind-branched), `abandon`, `hudInfo`, `target`/`field` (city vs. airport waypoint), `setLivery`, `force(fromId,toId)` charter test hook, guide arrow in `update`/`toggleArrow`.
- `shop.js` — `SHOP` catalog, knob arrays (`ENGINE_CAP`/`TIRE_*`/`LIGHT_I` — index 0 = stock, match vehicle.js), `PAINTS`/`PAINT_PRICE`, `buy`/`buyPaint`/`gearLevel`, `applyGear` (→ `player.perks` + bodyMat + dog; `perks.avionics` = aviation-band radio).
- `dog.js` — `DogSystem`: `update` (bed perch vs WALK follow; `FOLLOW_D`/`MAX_SPD` knobs), `honked` (delayed yips), `setOwned`, `mkDog`.
- `springer.js` — `SpringerSystem`: Sky, Cedar Park's "special friend" (not gear-gated, not a wildlife species): `update` (wander/approach state machine leashed to `ROAM_R` of home, `APPROACH_R`/`PET_R` knobs at top), `interact`/`nearHint` (E-key pat), `gY` (layered ground sampling), `mkSpringer`.
- `rabbits.js` — `RabbitSystem`: Maggy & Chowns, Georgetown's pair (same "special friend" idiom as springer.js, replicated not shared — WALK-mode-gated frolic instead of settle-and-wag): `updateOne` (frolic orbit re-clamped to `ROAM_R`, vehicles ignored outside WALK), `mkRabbit` (per-name fur/ear color, scaled up from the common jackrabbit).
- `travel.js` — `TravelMenu`: `render` (tab dispatch), `renderJobs`, `renderShop`, `buyItem`, `jobClick`, `go` (arrival modes), `NATURE`/`ICONS` POI tables.
- `hud.js` — `HUD`: `update` (12 Hz; location/road/speed/mode/forecast lines), `renderMapLayer` (one offscreen canvas; wide layer adds the dashed Tidelands contour — marching squares on `coastDist`, midpoints on `hud.tidelands`), `drawMini`/`drawBig`/`drawCompass`, `uiScale` (rem root), `toast`/`dialog`/`interactHint`/`subtitle(text, header)` (radio, one-line queue, `#radio-header`/`#radio-text`), `updateTags(sources, camera)` + `tagLabel`/`tagPool` (A5 aircraft tags over `radio.sources`), `toggleBigMap`/`toggleHelp`/`cycleZoom`. Road shield: `drawShield` (parses + rasters, cached on `ref+night` key, bumps `_shieldRaster`, `SHIELD_HOLD` grace timer bridges `nearestRoad` dropouts) → `drawInterstateShield`/`drawUsShield`/`drawCircleShield` (day = chrome via `chromeExtrude`/`chromeFace`/`specularStreak`/`bevelStroke`; night = dark `nightFace` + glowing `amberText` + `nightWireframe` edge, `AMBER`/`AMBER_LIT`; interstate also gets `nightBanner` — deep-navy band + dark-red stripe backlit at the seams), `animateShield(player, dt)` (per-frame steer-sway + idle yaw-rock CSS transform on the inner `#road-shield-card`, `shieldSway`/`shieldNight`).
- `audio.js` — `AudioSystem`: `update` (engine/wind/rain/crickets/theremin), `chime` (`SONGS` table), `radio(text, opts)` (tower-radio synth; `radioDuck` gain node, `opts.ufo` chop, `opts.voice` {p,r} per-type pitch/rate), `heli(dist)` (rotor ambience, 12 Hz-chopped bandpass noise; `heliTarget` — the last commanded gain, for tests), `honk`/`bark`/`howl`/`rattle`/`gobble`/`trainHorn`/`thunder`/`flare`/`step`, `toggleMute`.
- `traffic.js` — `TrafficSystem`: `update` (follow/brake/honk/pull-around; `DENSITY_DIVISOR` etc. at top; `gAt` = `this.groundYAt` callback ?? `hAt`, wired by main.js to airport+brand pad height since brands.js already imports this file and can't be imported back), `refreshCandidates` (supply-based density), `junctionHop`, `spawn`/`pickType`/`mixAt`, vehicle builders `mkSedan`/`mkPickup`/`mkSuv`/`mkSemi`.
- `animals.js` — `SPECIES` table (speeds/behaviors/night gates/facts; `leash` overrides the 45-unit wander cap — feedlot angus 2.2), `regionTable` (boxes mirror world.js, now split by `neighborStateAt` for band land — LA swamp/AR pine/OK plains/NM desert, Band Parity W5), `censusTable` (agAt-scaled horse/goat/sheep rows, drawn after regional rows; appends `RANCH_ARCHES` boost rows — radii in chunk-MID space, ≥200, coords sync with gameplay.js), `AnimalSystem`: `update`/`flee` (heading: away = atan2(-dx,-dz))/`scare`/`sound`, `spawn` (also homes herds at `farmsteadAt`/`feedlotAt` sites via `agAt || bandAgAt` + `BISON_SITE` Caprock herd + `ARANSAS_SITE` Blackjack Peninsula birds (spoonbill flock + crane pair, `aransasflock` stream) + wave-5/5b `ranchHQAt` signature herds — santagertrudis/axisdeer/blackbuck/hereford, `SIG` table keyed by sig, 6666 horses homed in the pens, JA bison, XIT longhorns, Matador/LBJ herefords, `inTexas`-only by design), wander/flee/road-avoidance are `inTexasOrBand`/`nearestAnyRoad` throughout, `mkAnimal`.
- `npcs.js` — `NPCSystem`: `interact` (dialog assembly; B2 heli/job/forecast context pulls; `ag: true` looks swap weather openers to `AG_OPENERS` — 5 rural ag characters placed by baked coords, not city name), `startle`, `spawnTownsfolk`, `spawnBystanders`/`bystanderConvo` (B1 gate figures, `gatefolk:` stream, `nextSlot` schedule scan, `this.aviation` assigned by main.js), `roadShoulder`, `mkCharacter`, `gY` (module-level ground height: airport pad ?? brand pad ?? raw `hAt`), `POOLS` (all content pools, verify surface; `PROFESSION_LINES` flavored smalltalk, `BYSTANDER_ROLE_INFO` variant age bands).
- `cities.js` — `CitySystem`: `spawn` (InstancedMesh buildings, `hasRealStreets`), `setNight` (window glow), `cityRadius` (visit radii).
- `brands.js` — `BrandSystem`: real-coord Texas brands as parody landmarks, CitySystem-style proximity streaming (`SPAWN_DIST` 700, `update(px,pz,dt)` scans the hand-authored table, `spawn`/`despawn` per-site; shared `heroMat`/`propMat` + shared prototype geos in `this.shared`, disposed never). Wave 1 = Bucky's (Buc-ee's): `BUCKY_SITES` (15 real OSM coords), `buildBuckyHero` (storefront + fuel canopy + sign pylon + `beaverParts`; one static merged geo, plain diffuse colours), `mkPump`/`mkBillboard` prototypes, `buildBillboards` (approach signs hugging the nearest motorway/trunk). **Night lighting = two persistent warm `PointLight`s** (`canopyLight`/`signLight`, lantern/headlight precedent — NOT emissive: white emissive can't keep a yellow sign yellow; Bruno's directive 2026-07-12) repositioned in `update()` to the NEAREST live site's world anchors (`CANOPY_ANCHOR`/`SIGN_ANCHOR`, transformed by the group's Y-rotation trig, not `localToWorld` — stale matrixWorld) and faded by `ATMOS.night`; created/added once (adding scene lights at runtime recompiles shaders). Wave 2 = **H-E-Buddy** (H-E-B): `buildHEBSites` (33 largest `GEO.cities`, seeded angle/radius search snapping to a city-edge road shoulder clear of the downtown footprint + `airportClear`), `buildHEBHero` (big-box + red sign band + curved entry canopy + dock), `mkCartCorral`/`mkCart`/`mkLightPole` instanced lot props, `hebSignTex`; night = a third persistent `PointLight` (`hebSignLight`, same pool). Wave 3 = **Lone Star Compute** (AI datacenters): `LSC_SITES` (8 real 2026 campus towns), `buildLoneStarHero` (two windowless ribbed-roof server sheds + office + fence + substation + instanced-pylon transmission line with a sagging-wire hint), `mkCoolingFan`/`mkCondenserDrum`/`mkPylon` instanced props, `spawnLSC`; night glow is the track's ONE **emissive** exception — shared `ventMat` (dark diffuse + saturated cold-blue emissive, `emissiveIntensity` toggled by `ATMOS.night` in `update()`, cold-vs-warm contrast with the signage lights); `onHum(dist)` callback (set on the instance by main.js) drives `audio.datacenterHum` with the nearest live-site distance. `update()` finds the nearest live site per brand three ways (`rec.type` `'bucky'`/`'heb'`/`'lsc'`). Imports geo+sky+traffic (tinted/merge) + cities (`cityRadius`) + airports (`airportClear`). `groundYAt(x,z)` (module export, airport-pad idiom over `BUCKY_FOOT`/`HEB_FOOT`/`LSC_FOOT` rotated-rectangle footprints — read directly by vehicle.js/npcs.js, wired into traffic.js as a callback to dodge the import cycle) so players/NPCs/cars ride a site's foundation slab instead of sinking through it. **Player-controlled global size** (all three brands, one factor — `[`/`]` in main.js): module-level `SCALE` (own `lonestar-brand-scale` localStorage key, ui-scale idiom, clamped 0.1–1.25, NOT gameplay.save), `BrandSystem.setScale(v)`/`.scale` getter — despawns every live site so the next real-loop tick rebuilds at the new size. Each `spawn*()` parents its hero/signPanel/props into an inner `building` sub-group (stored on the live record) that carries `.scale`, so the pad/pylon/props resize about the pad origin together; approach billboards are NOT in `building` (scaling `group` itself would drag them out of position) — each billboard's own local group gets `.scale` instead, resizing in place on its own terrain-sampled base. `footAt` multiplies `foot.hx`/`z0`/`z1`/`PAD_TOP` by the live `SCALE` so the walkable floor always matches the rendered slab; the two Bucky's/H-E-Buddy light anchors (`toWorld`) multiply by `SCALE` too so night lights stay glued to the rescaled sign/canopy.
- `airports.js` — `AIRPORTS` table (27 fields — 7/15/5 by tier, incl. 6 band fields from Shoulder & Shelf W2 (SHV/TXK/CVN/HOB civilian + CVS/BAD military); **every new field also needs a ROUTES entry in aviation.js** or daySchedule crashes at boot; true headings from OSM; `RW_W`/`MARGIN` tier knobs; pure per-site `anchor`/`gate` computed at module load), `airportClear` (pure footprint exclusion — called by cities.js/ScenerySystem/`chapelAt`), `onRunway` (pure pavement-corridor test; `clearOfRunways` is its negation), `TD_AGL`/`TD_SPD` (touchdown thresholds — the shared definition of "landed", read by radio.js and missions.js), `airportLayout` (pure, pads at max `hAt`; `padYOf` single-site scan), `padAt(id)` (pure helipad spot beside the anchor — A4 medical pad stops land here), `windFrom` (per-day seeded wind direction), `runwayInUse` (pure argmax end into `windFrom` — AI ops + radio.js ATIS share it), `rwyLabel` (spoken runway number — ATIS + B1 gate spotter), `AirportSystem` (8 merged meshes in `this.group`; beacon spin + windsock droop in `update`).
- `aviation.js` — `daySchedule` (pure seeded departures; `SLOTS`/`REDEYE_MAX`/night-u window consts), `AviationSystem`: `buildRaw` (closed-form trajectory legs; speed/altitude knobs in `TYPES`/`SLOPE`/`CEIL`), `evalFlight`, `update` (materialize scan within `MAT_R`, `MAX_AIR` cap, storm/dust ground stop + `divert` go-around, dt-vs-days delay model), `divert(m)` (public go-around trigger, reused by radio.js), `force('departure'|'arrival')`, `despawnAll`, `ROUTES` (real weighted pairs), `mkJet`/`mkGa`, `simT` (real-loop sentinel).
- `aviation-rules.js` — `routeProblems`/`pickRoute`/`scheduleAirport` (dependency-free route validation and seeded-slot shape called by `aviation.js`).
- `mission-rules.js` — `groundOfferTerms`/`charterOfferTerms`/`missionPayout` (dependency-free offer economics called by `missions.js`).
- `radio.js` — `TowerRadio`: `ctxFor` (live tokens per source; W7's located `nm`/`shelf` gate the west/offshore winks), a new chatter kind needs a `ROLL_OK` row as well as a pool + voice or it never speaks; `receivable` → `{a, kind: 'tower'|'unicom'}` (FLY-in-range — 250u tower/120u UNICOM — or `perks.avionics`), `update(dt, player, aviation, sky)`, `atis`/`awos`/`narrateOps`/`playerFlow` (tower only)/`checkBlock` (physical go-around, reception-independent, all 20 fields), `tx`/`lastTx` (structured, for tests; A3 adds `{at, src, cs, route, phase, casual, voice, header}`), A3 chatter: `scan` (→ `this.sources`, `DIRECT` 60u window — one enumeration for chatter + hud tags), `chatter` (`EDGE_EV` phase edges, `ROLL_OK` enroute rolls, `GAP_MIN`/`GAP_VAR` 25–45 s budget, `OPS_HOLD` preempt, `playerRefT` throttle + `REF_SPD` gate), `ctxFor` (live template context), `helis`/`militaryAir` (assigned by main.js), `TOWERED`/`TOWERED_COUNT` (7 tier-1 hubs), `UNICOM` (9 tier-2 fields), `nearestTowered`/`nearestUnicom`.
- `chatter.js` — A3 line pools, pure (geo.js only): `VOICES` ({p,r} synth character per type, consumed by audio.radio), `HELI_ID` (kind → callsign/operator/voice: Lifeguard 3/StarCare, Chopper 5/KTX News 5, Rescue 6-0, Hood 2-1), `POOLS` (kind×event templates, `{token}`-gated = factual by construction), `AIRLINE_POOLS` (per-carrier registers), `chatterLine(kind, event, ctx, seed)` (seeded `chatter:` pick, ~1-in-4 humor rationing).
- `rotors.js` — `HeliSystem` (medical/news/coastguard/army candidates — each exposes `ph`/`city`; medical adds `fieldId`/`pad`/`sortieN`, `force(kind, {pad})`/`despawnAll`/`airborneCount`/`nearestAirborneDist`, `startSortie` (seeded `padstop:${city}:${day}:${sortie}` roll, `PAD_ODDS`/`MED_SPD`/`PAD_DESC_T` knobs), `advancePadSortie` (transit→descend→pad→lift→return timeline), global `CAP=2` incl. weighted Army pair, `update(dt,px,pz,days)` real-loop `simT`), `BlimpSystem` (`positionAt(day,u)` pure per-day loiter query, `loiterAnchor`/`loiterParams`, `update(dt,days)` loiter/transit/moored state machine, `simT`).
- `military.js` — `MilitaryAirSystem` (wave 5): `candidates` ('nasa' rare arrival into Ellington, 'lowlevel' rare fast pass gated to the Trans-Pecos box `x < -2200`), `force(kind, aviation)`/`despawnAll`/`airborneCount`/`nearestAirborneDist`, shares `aviation.js`'s `MAX_AIR` fixed-wing cap via `aviation.airborneCount()` (own weight `PAIR_W=2`), `update(dt,px,pz,aviation)` real-loop `simT`.
- `flares.js` — `FlareSystem`: `fire`/`snuff`/`update` (ballistic→chute phases; `LIGHT_I`/`BURN`/`CHUTE_FALL`/recharge knobs at top).
- `trains.js` / `maritime.js` / `ufo.js` / `bats.js` — `TrainSystem` (`arcInit`/`at` arc-length follow, `mkLoco`), `MaritimeSystem` (`laneAt`, `buildPorts`/`buildPlatforms` (Energy W2 rebase: `platforms` = the 227 baked `GEO.energy.platforms` records drawn as per-component InstancedMesh; `farSite`/`farMiles`/`farRig` — bespoke upgraded group at the farthest `inWorld` real major)/`buildFairwayLegs` (`fairwayLegs` port-approach polylines snapped to baked fairway points; a `leg`-keyed tanker pingpongs the longest)/`buildShips`/`buildBuoy` (Tidelands nun buoy), `plaques`/`plaqueNear` (buoy + Far Rig brass — Far Rig name/operator/miles templated from `farSite`), `rigGlow`/`workGlow` night-gated glow mats — `fog: false`, opacity = `ATMOS.night`; Energy W4: `spillMat` + `fadeDisc(geo,r)` (exported radial vertex-color falloff for additive glow discs) water spill under the fleet, major decks + Far Rig flame register sky light-pool anchors), `UFOSystem` (`hotspotBoost`, `startSaucer(px,pz,immediate)`/`startFormation`; hover *stalks* the player — standoff/height/orbit knobs in the `tgt` block, `near` ramp drives the Levelland flicker), `BatSystem` (dusk window on `sky.t`).
- `shoulder.js` — W6a+6b line vignettes + crossing ceremony: `ShoulderSystem` (static boot build, airports.js idiom), `deriveCrossings` (band-stub endpoints on the border → monument sites; Mexico filtered via `neighborCountyAt` probe), `buildMonuments` (granite WELCOME slabs + bluebonnet beds, `bluebonnet` stream), `buildStones` (7 Corner Stones snapped to border vertices; proximity → `onStone` → `gameplay.stampStone`), `buildNeutralGround` (everything anchored to the I-10E stub: cypress/moss rows on the LA bank (`cypress` stream), crawfish ponds, Vinton fireworks barns, Neutral Ground marker, control-city signs incl. US-71 Natchitoches), `buildTexarkana` (State Line Ave deck — NOT a road, causeway precedent; two-state federal building; straddle toast), `buildCasino` (WinBig World, plates InstancedMesh `userData.plates='TEXAS'`, `winbig` stream), 6b west via `buildWest`: `buildTexola` (ruins, `texola` stream), `buildGlenrio` (two-faced FIRST/LAST sign, `glenrioSign`), `buildTexhoma` (elevators + painted 36.5°N line), `buildAnthony` (leap-year banner, sagged planes), `buildDoorstep` (Whites City strip anchored to the US-62 stub + `parkRoad` switchback ribbons + entrance sign, zero cave) + west I-40/I-10 control signs, `ribbon` (arbitrary-bearing drapedPlane), `plaqueNear` (4th source in main.js's unified lookup, `icon` field; 15 plaques), `swampAt` (pure frog-country factor → `audio.swamp`), `shoulderClear` (pure footprint standoff, consumed by cities.js placement), shared `glowMat` (fog:false, opacity = ATMOS.night) for the 4 beyond-band horizon glows (E Lake Charles/Natchitoches, W Lawton/Alamogordo).
- `turtles.js` — `TurtleSystem`: Malaquite dawn Kemp's ridley release (`NEST`/`WINDOW`/`ODDS`, seeded per game day on `turtle:` stream, logs the species within `SPOT_R`; `mkHatchGeo` merged vertex-colored hatchling, `onEvent` toast once per release morning within `HAIL_R`).
- `haunts.js` — `LEGENDS`/`LEGEND_COUNT`/`EROCK`, `HauntSystem.update` (wisp arm/fade, ghost fires, midnight bell via `lastBell`, 1554 treasure light — `TREASURE_AT` off the Mansfield Cut, new-moon gate matching sky.js's label, recedes via `tPos` but never leaves `inStateWater`, `T_*` knobs at top; debug `force` flag; gates/radii knobs at top: `WISP_ODDS`/`NIGHT_MIN`/`FADE_*`/`WATCH_*`/`BELL_R`); chapel sites come from world.js `chapelAt`/`chapelSitesNear`.
- `energy.js` — `EnergySystem` (Energy W2): `HEROES`/`ENERGY_TOTAL` (hand-authored real hero sites — Spindletop, Midland Tank Farm, W4's 4 refinery heroes with `site` linking their baked record — built once in `buildHeroes`), `register(x,z,r,label)` announcer registry (armed per site, hysteresis re-arm at 1.6r, 6 s cooldown, nearest named wins; W2 registers heroes + platforms name→operator→silent, W4 named refineries minus hero-claimed), `update` (0.4 s cadence: announce via `onToast` + hero `gameplay.logEnergy` inside 12 u; W4 flame flicker + `spillMat` night gate), W4 `buildRefineries` (all 33 baked sites → 4 merged meshes via world.js `mergeGeoms`; per-prop road/river clearance scaled by prop extent with seeded retry placement, heroes ×1.7 with cracker/6-tank/twin-flare dressing, flames on shared `scenery.flareMat`, flare+refinery glow anchors into sky pool, draped `fadeDisc` ground spill), `plaques`/`plaqueNear` (hero brass, main.js unified lookup).
- `debug.js` — `initDebug` playtest menu (`?debug=1` + backquote): `actions` table (day/night/midnight, `hauntCemetery`, `ghostFires`, `saucer`/`formation`, `bats`, weather picks) + `visit(spot)` tour teleporter, both always on `__game.debug`; panel has Actions/Tours/Perf tabs, only the panel is URL-gated.
- `perf.js` — `PerfMonitor` (`__game.perf`): `frame`/`lap(name)` timing between main.js's sequential updates (EMA avg + max + tick count `n`, zero steady-state allocation), `snapshot`, `resetMax`, `captureRender`/`renderProbe` (one true frame under `__skipRender`); harness fake clock zeroes ms — assert structure/ticks, read real ms in the debug Perf tab.
- `tours.js` — `TOURS` pure data: per-track/per-wave playtest teleport spots (`{label,x,z,heading?,mode?,time?,weather?,act?,note?}`) rendered by debug.js's Tours tab; every wave appends its spots (protocol step 4).
- `main.js` — `boot`: system construction order, keydown map (horn/interact/travel/…), render loop (update call order, 12 Hz `hudTick` block, `__skipRender` gate), `window.__game` exposure.

## tools/

- `verify.mjs` — harness: `check`, `t.ev/tp/wait/simWait/simStep/step/hold/release/key/until/setTime/setDay/setNight/setWeather/sample/shot/stubGamepad`; compact output default, `-v` verbose; `__skipRender` set at boot.
- `checks/*.mjs` — suites: debug/drive/haunts/hud/lights/missions/shop/traffic/wildlife (one default-export async fn each; debug runs first alphabetically and must restore daylight/clear weather).
- `build-data.mjs` (`proj` — duplicated as `LL` in gameplay.js/travel.js), `build-elevation.mjs` (grid consts ↔ `ELEV`), `build-sky.mjs`, `add-metro-streets.mjs` (append-only), `status.sh`.
